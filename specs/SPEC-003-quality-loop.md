# SPEC-003：独立 Agent 质量收敛循环

- 状态：`Implementing`
- 创建：2026-09-01
- 最后更新：2026-09-02

## 1. 目标

通过彼此独立的产品、后端和体验审查，持续发现一个最值得实现的需求或最有必要修复的 BUG；每轮只引入一个主要变量，完成规格、开发和验证后再进入下一轮，直到项目质量收敛。

## 2. 每轮流程

1. 启动必要数量的独立 agent，默认覆盖产品价值、数据正确性、交互/可访问性。
2. 每个 agent 只推举 1 项，必须包含证据、用户影响和验收标准，不直接改代码。
3. 主 agent 按以下顺序选择本轮唯一最高优先级项：
   - P0 数据丢失、安全或服务不可用
   - P1 核心流程错误、并发一致性、明确需求不可用
   - P2 显著提升完成率或可访问性的功能
   - P3 纯视觉或便利性优化
4. **先更新对应规格**，写入复现路径/需求和验收标准。
5. 开发、添加回归测试、完成构建和浏览器验证。
6. 将验证证据回写规格，然后开始下一轮独立审查。

## 3. 停止条件

质量循环不得因“核心功能已经存在”或单轮未发现严重问题而提前结束。只有同时满足以下全部条件时才能终止：

- **连续两轮**独立审查均没有发现新的 P0/P1，也没有仍然有明确用户价值、逻辑合理且可在当前产品边界落地的 P2。
- 每轮至少覆盖产品流程、数据/API 正确性、浏览器交互与“页面承诺—真实功能”四个视角；审查者必须提供证据，不能只运行现有测试后宣布无问题。
- 从用户视角完成“发起 → 认领/自领 → 准备 → 排期 → 参会/宣讲 → 归档 → 查看沉淀”的端到端旅程，并明确每个状态的允许动作、撤销/纠错路径和失败反馈。
- 页面上所有按钮、链接、导航、状态提示和功能性文案均已建立“可见承诺 → 规格 → 实现 → 浏览器验收”的追溯；不能存在有文字无功能、死入口或名不副实的统计/状态。
- 没有未实现的用户明确需求；新增的合理功能完成规格、实现、自动化、桌面/移动浏览器验收和部署验证。
- 当前全部规格的自动化和浏览器验收通过，依赖审计没有 moderate 及以上漏洞，生产服务健康。
- 剩余项只能是低价值视觉偏好、需要用户业务决策/外部权限，或明确超出产品定位；每一项都必须写明证据与不实施理由。

终止时必须输出完整的功能成熟度清单、验证证据和剩余边界，不能默默忽略，也不能以时间、工作量或循环轮数作为停止理由。

## 3.1 页面承诺审查清单

每轮浏览器审查至少逐项检查：顶部导航、Hero/Closing CTA、统计卡片、状态筛选、搜索、排序、列表卡片动作、月/周日历、弹窗、资料/会议链接、流程说明和 Footer。任何暗示可操作或可到达结果的文字，都必须满足以下之一：

1. 入口可操作并完成对应业务结果，有成功/失败反馈和浏览器验收；
2. 明确标注为站外/线下说明，不伪装成站内功能；
3. 删除或改写误导性文案。

## 4. 第 1 轮候选（2026-09-01）

| 审查方向 | 推举项 | 优先级 | 处理 |
| --- | --- | --- | --- |
| 产品 | “本周排期”名不副实，缺少真实月/周日历 | P1 | 已纳入 SPEC-002 |
| 后端 | 生命周期先读后写，跨进程存在覆盖竞态 | P1 | 已按 BUG-CONC-001 修复 |
| 体验 | 整卡拖动对触屏/键盘不可用 | P1 | 已纳入 SPEC-002 FR-SORT-002 |

本轮在完成 SPEC-002 后，优先修复生命周期原子转换，再开启下一轮复审。

### BUG-CONC-001 生命周期转换竞态

复现条件：两个连接同一 SQLite 文件的服务实例，同时读取同一 `OPEN` 议题并提交不同认领人。旧实现先 `SELECT` 校验、再按 `id` 无条件 `UPDATE`，可能让两个请求均成功，最终由后写者静默覆盖。

修复规格：

- 认领必须使用 `UPDATE ... WHERE id = ? AND status = 'OPEN'`。
- 排期必须使用 `UPDATE ... WHERE id = ? AND status = 'CLAIMED'`。
- 归档必须使用 `UPDATE ... WHERE id = ? AND status = 'SCHEDULED'`。
- 以 SQLite `changes` 作为唯一成功依据。
- `changes = 0` 时复查 ID：不存在返回 `404`，存在但状态变化返回 `409`。
- 不能仅依赖单进程同步执行或普通 deferred transaction。

验收：

- 两个独立应用实例连接同一临时数据库，并发认领同一议题时，恰好一个 `200`、一个 `409`。
- 最终 `presenter` 等于唯一成功响应中的认领人。
- 排期和归档继续遵守状态机，现有生命周期测试全部通过。

## 5. 验证记录

- 第 1 轮三项 P1 均已完成：真实月/周日历、触屏/键盘排序、生命周期 CAS 原子更新。
- 双应用实例并发认领测试验证恰好一个 `200`、一个 `409`，最终认领人与成功响应一致。
- 下一步：部署 SPEC-002 后启动第 2 轮独立审查。

## 6. 第 2 轮候选（2026-09-01）

| 审查方向 | 推举项 | 优先级 | 本轮决策 |
| --- | --- | --- | --- |
| 产品 | PATCH 全行回写与生命周期并发时丢失排期/归档字段 | P1 | **选中，优先修复** |
| 后端 | 手动排序缺少版本 CAS，陈旧客户端覆盖新顺序 | P1 | 保留至下一轮 |
| 体验 | 多个排序保存请求乱序，UI 与数据库分叉 | P1 | 与后端项同源，保留至下一轮 |

选择理由：PATCH 丢失更新会破坏 `SCHEDULED`/`ARCHIVED` 状态的数据不变量，造成排期从日历消失或归档无摘要；比排序顺序冲突具有更高的数据完整性风险。

### BUG-CONC-003 PATCH 全行丢失更新

复现：PATCH 先读取 `CLAIMED` 整行快照并暂停；另一实例完成排期；PATCH 随后把旧快照中的空排期字段与标题一起全量写回，最终记录状态为 `SCHEDULED`，但 `scheduled_at/duration/room` 为空。

修复规格：

- PATCH 只生成请求实际提交字段的白名单 `SET` 列，不得回写未提交字段。
- `updated_at` 始终更新。
- 状态权限仍按读取时状态校验；并发生命周期动作更新的其他列必须保留。
- 更新期间记录被删除时返回 `404`。

验收：

- 两个共享数据库的应用实例通过测试钩子形成可控交错：PATCH 标题读取后暂停，另一实例完成排期，再恢复 PATCH。
- 两个请求均成功时，最终标题为新标题，状态为 `SCHEDULED`，且分享人、时间、时长、地点全部保留。
- 同理，普通字段编辑不得清除归档摘要、资料或归档时间。
- 现有全部单元/API/E2E 继续通过。

实现与验证：

- PATCH 已改为根据请求字段生成白名单动态 `SET`，不再合并并回写整行快照。
- 新增测试钩子形成确定性交错：编辑读取后暂停 → 第二实例排期 → 恢复编辑。
- 最终新标题与 `SCHEDULED` 状态并存，分享人、时间、50 分钟时长和地点全部保留。
- 12 项单元/API 测试、桌面 5 项 E2E、移动 3 项 E2E、生产构建和 0 漏洞审计通过。
- 状态：Accepted；实现提交 `33baec0`。

## 7. 第 3 轮结论（2026-09-01）

三位独立审查者均未发现 P0，并一致推举第 2 轮保留项为唯一 P1：手动排序缺少版本 CAS，且前端允许多个保存请求乱序完成。

### BUG-CONC-004 排序陈旧写与 UI/DB 分叉

复现 A（多客户端）：两个客户端都读取版本 `r` 和顺序 `[1,2,3]`；甲提交 `[2,1,3]` 成功，乙仍基于 `r` 提交 `[1,3,2]` 也成功，静默覆盖甲。

复现 B（单页面乱序）：R1 上移请求被延迟，R2 后续移动先成功，R1 最后成功或失败；UI 使用 R2 的乐观顺序，数据库可能保留 R1，或 R1 的失败回滚覆盖 R2，刷新时发生跳变。

后端修复规格：

- 新增单例顺序元数据 `manual_order_version`，旧库从 `0` 开始。
- `GET /api/topics` 保持数组响应，并通过 `X-Order-Version` 返回当前非负整数版本。
- `POST /api/topics/reorder` 必须提交 `baseVersion`。
- 在同一个 `BEGIN IMMEDIATE` 事务中完成：读取/校验版本、校验完整 ID 集、写入连续唯一 position、版本 `+1`。
- 陈旧版本返回 `409`，并在响应头返回当前版本；不得修改任何 position。
- 创建在立即写事务中分配末尾位置并推进版本，防止并发重复 position。
- 删除在立即写事务中删除、压实连续 position 并推进版本。
- 位置重排采用安全的两阶段临时值，允许数据库建立唯一索引。

前端修复规格：

- 保存 `X-Order-Version`，重排携带当前 `baseVersion`。
- 使用同步锁保证任一时刻最多一个 reorder 在途；保存期间拖动、上移和下移全部禁用。
- 成功后使用响应头的新版本继续后续操作。
- 409 或其他失败均先恢复操作前快照，再拉取服务端权威手动顺序和版本；明确提示冲突/失败。
- `aria-live` 宣告保存中、成功位置或已同步最新顺序。

验收：

- 两个共享数据库的应用实例基于同一版本提交不同顺序，恰好一个 `204`、一个 `409`；最终顺序等于成功者，position 为 `1..N`。
- 快照后发生创建或删除，旧 reorder 返回 `409`。
- 浏览器延迟首个请求时，全部排序控件禁用，强制第二次操作不得发出第二个请求。
- 请求完成后 UI、`GET` 和刷新结果一致；失败路径重新同步权威顺序。
- 全部既有测试继续通过。

## 8. 第 3 轮状态

- BUG-CONC-004：Accepted。
- 后端：`topic_order_state` 持久化版本；读取用响应头返回版本；创建、删除、重排均使用立即写事务并推进版本；position 建立唯一索引并以两阶段临时值安全换位。
- 并发：双应用同版本重排恰好一个 `204`、一个 `409`；创建或删除后旧快照重排均为 `409`，position 始终连续。
- 前端：同步锁和禁用状态保证单飞；成功接收新版本；失败拉取服务端权威顺序并提示。
- 验证：13 项单元/API 测试；桌面 6 项 E2E、移动 4 项 E2E；生产构建和 0 漏洞审计通过。
- 实现提交：`9a7d72c`。

## 9. 第 4 轮候选（2026-09-01）

| 审查方向 | 推举项 | 优先级 | 本轮决策 |
| --- | --- | --- | --- |
| 产品 | 删除全部议题后重启，演示数据重新出现 | P1 | **选中，优先修复** |
| 后端 | 列表顺序与顺序版本不是原子快照 | P1 | 保留至下一轮 |
| 体验 | Dialog 缺少焦点圈、Esc 和焦点恢复 | P2 | 不阻止本轮，保留 |

选择理由：清空后重启是正常 CRUD + 部署路径下 100% 可复现的数据语义错误，直接违反永久删除预期。

### BUG-DATA-005 演示数据在清空后复活

根因：生产默认启用 seed，而初始化仅用 `COUNT(topics) === 0` 判断；用户合法删除最后一条议题后，下次启动会被误判为全新数据库。

修复规格：

- 新增持久化 `app_state(key, value)`，使用 `sample_data_initialized` 一次性标记。
- 标记不存在时才执行初始化决策：
  - `seed=true` 且业务表为空：插入演示数据；
  - `seed=false` 或已有业务数据：不插入；
  - 无论是否插入，均写入初始化标记。
- 标记存在时不得因业务表为空再次插入样例。
- 旧数据库升级时若已有业务数据，仅补标记，不改数据。

验收：

- 同一临时数据库首次 `seed=true` 得到 4 条样例；通过 API 删除全部，关闭并按相同配置重开后仍为 `[]`。
- 全新数据库首次 `seed=false` 保持为空；关闭后以 `seed=true` 重开仍为空。
- 旧数据库已有数据、没有标记时升级只补标记，不新增样例。
- 全部现有测试继续通过。

状态：Accepted。

实现与验证：

- 新增 `app_state` 与 `sample_data_initialized` 一次性标记。
- 首次启动无论 seed 开关或是否已有数据，都会固化初始化决策；后续不再用业务表为空推断。
- 15 项测试通过，包含 seed 后清空重启仍为空、首次禁用后重启仍为空、旧库无损升级。
- 桌面 6 项、移动 4 项 E2E、生产构建和 0 漏洞审计通过。
- 实现提交：`4f6cdf1`。

## 10. 第 5 轮候选（2026-09-01）

| 审查方向 | 推举项 | 优先级 | 本轮决策 |
| --- | --- | --- | --- |
| 产品 | 列表与顺序版本不是同一个数据库快照 | P1 | **纳入本轮端到端修复** |
| 后端 | 审查未完成（工具安全策略中止） | — | 由产品审查的确定性复现和主 agent 复核覆盖 |
| 体验 | 过期排序请求覆盖当前列表，可把错误来源持久化为手动顺序 | P1 | **纳入本轮端到端修复** |

选择理由：两个缺陷发生在同一条“读取排序快照 → 编辑 → CAS 保存”链路。只修服务端仍会允许浏览器把其他排序模式的列表作为手动顺序提交；只修前端仍会接收服务端混合快照并绕过版本冲突。因此本轮以“排序读取快照端到端一致性”为单一质量目标，同时封闭服务端快照边界和客户端请求来源边界。

### BUG-CONC-006 列表与顺序版本的非原子快照

复现：实例甲的 `GET /api/topics` 先读取旧顺序 `[1,2,3]` 后暂停；实例乙完成重排并把版本从 `r` 推进至 `r+1`；实例甲再读取版本，返回“旧列表 + 新版本”。客户端基于这组混合数据重排时能通过 CAS，静默覆盖实例乙的结果。

服务端修复规格：

- `GET /api/topics` 的议题行与 `X-Order-Version` 必须来自同一个 SQLite 只读事务快照。
- 快照读取不能阻止另一数据库连接在 WAL 模式下完成写入。
- 查询失败时必须回滚已开启的读取事务，连接不得残留事务状态。
- 所有排序和状态筛选模式都遵守同一快照规则。

服务端验收：

- 两个应用实例共享数据库；实例甲读取 rows 后由测试钩子暂停，实例乙完成重排并推进版本，再恢复实例甲。
- 实例甲只能返回“旧列表 + 旧版本”或“新列表 + 新版本”，不得返回混合快照。
- 若实例甲拿到旧快照，随后以该版本重排必须返回 `409`，且不得覆盖实例乙的权威顺序。
- 创建或删除与 GET 交错时同样遵守快照一致性；既有 CAS 回归全部通过。

### BUG-UI-007 过期排序请求污染当前手动列表

复现：手动顺序为 `[A,B,C]`、最新创建排序为 `[C,B,A]`。延迟 `sort=newest` 响应，快速切换“最新创建 → 手动排序”；新的 manual 请求先完成，旧 newest 请求后完成。旧实现仍回写旧响应，导致选择框显示“手动排序”但列表为 `[C,B,A]`；一次上移/下移即可把错误来源以合法版本永久保存。

前端修复规格：

- 每次列表加载分配单调递增请求序号；只有最新请求可以原子提交 `topics + orderVersion + loadedSort`。
- 过期请求不得更新议题、版本、加载态或错误态；统计响应可以独立更新，不受议题排序请求竞争影响。
- 手动重排必须同时满足当前 `sort === 'manual'`、已提交快照的 `loadedSort === 'manual'`、最新列表加载已完成且没有重排请求在途。
- 排序冲突后的权威同步也必须成为最新快照，不能再被先前列表请求覆盖。

前端验收：

- 浏览器确定性延迟旧 `newest` 请求，在切回 manual 且新请求完成后释放旧请求；列表和版本保持 manual 响应，旧响应被忽略。
- 过期请求完成后加载状态和错误提示不回退。
- 重排请求的 `orderedIds` 必须来自最近一次已提交的 manual 快照；当前快照来源不明或仍在加载时排序控件禁用。
- 既有拖动、键盘移动、失败同步和日历视图回归全部通过。

## 11. 第 5 轮状态

- BUG-CONC-006、BUG-UI-007：Accepted。
- 服务端：列表查询与顺序版本在同一个 deferred 只读事务中读取；异常路径回滚，不残留事务状态。
- 并发：双实例确定性交错中，写实例可在读暂停期间完成重排；读实例仍返回旧列表与旧版本，后续陈旧写收到 `409`，权威顺序不被覆盖。
- 前端：议题与统计请求独立编号；只有最新且排序来源匹配的议题响应可提交；`loadedSort`、加载态和单飞锁共同控制手动排序。
- 浏览器：延迟旧 `newest` 响应、切回 manual 并先完成新请求后，旧响应不会回写；重排载荷来自最新 manual 快照。
- 验证：16 项单元/API 测试；桌面 7 项 E2E、移动 5 项 E2E（另 2 项按设备设计跳过）；生产构建和 0 漏洞审计通过。
- 实现提交：`8596c06`。

## 12. 第 6 轮候选（2026-09-01）

| 审查方向 | 推举项 | 优先级 | 本轮决策 |
| --- | --- | --- | --- |
| 产品 | 无新的 P0/P1 | — | 核心需求已闭环 |
| 后端 | 旧库重复 `position` 导致升级启动失败 | P1 | **选中，优先修复** |
| 体验 | 无新的 P0/P1；弹窗焦点管理、月历溢出入口等为 P2 | P2 | 保留至最终低优先级清单 |

选择理由：旧版本曾允许多实例用 `MAX(position)+1` 非事务创建议题，可能产生重复非零位置。当前迁移仅处理 `position = 0`，随后直接创建唯一索引；受影响数据库会稳定触发唯一约束错误，导致网站和 API 完全无法启动。

### BUG-MIG-008 重复位置旧库无法升级

复现：构造一个已有 `position` 字段但尚无唯一索引的历史数据库，至少两条议题的非零 `position` 相同；调用 `buildApp()` 时，旧实现执行 `CREATE UNIQUE INDEX idx_topics_position` 并抛出 `UNIQUE constraint failed: topics.position`。

迁移规格：

- 创建唯一索引前检查 `idx_topics_position` 是否已经是唯一索引。
- 唯一索引尚未建立时，在同一个立即写事务中按 `(position ASC, id ASC)` 读取全部议题，将位置稳定归一化为连续 `1..N`，随后创建唯一索引。
- 旧库缺少 `position` 字段时先添加字段，再走相同归一化流程；不能依赖一次性的 `position = id` 更新。
- 若存在同名非唯一索引，迁移应替换为唯一索引，不能因 `IF NOT EXISTS` 静默失去约束。
- 迁移不得修改标题、简介、人员、标签、状态、排期、归档或审计时间等业务字段。
- 迁移可重复执行；第二次及以后启动不得改变已归一化顺序。

验收：

- 自动化构造位置为 `[2,1,1]` 且含不同业务字段的旧库；首次启动成功。
- 迁移后按旧 `(position,id)` 相对顺序返回，位置严格为 `1..N`，所有业务字段逐项不变。
- `PRAGMA index_list(topics)` 显示 `idx_topics_position` 为唯一索引，尝试写入重复位置受到约束。
- 关闭并再次启动仍成功，顺序和位置不变。
- 既有缺字段迁移、CRUD、排序 CAS、只读快照与全部浏览器回归继续通过。

## 13. 第 6 轮状态

- BUG-MIG-008：Accepted。
- 迁移：缺少唯一索引时，在立即写事务内按 `(position,id)` 稳定归一化为 `1..N`，再建立唯一索引；同名非唯一索引先替换。
- 数据：重复 `[2,1,1]` 历史夹具首次启动成功，除 `position` 外的全部数据库列逐项不变；第二次启动顺序与数据不变。
- 约束：`PRAGMA index_list(topics)` 确认位置索引为唯一，重复位置写入被 SQLite 拒绝。
- 验证：18 项单元/API 测试；桌面 7 项 E2E、移动 5 项 E2E（另 2 项按设备设计跳过）；生产构建和 0 漏洞审计通过。
- 实现提交：`4c60f5a`。

## 14. 第 7 轮候选（2026-09-02）

| 审查方向 | 推举项 | 优先级 | 本轮决策 |
| --- | --- | --- | --- |
| 数据正确性 | 状态相关 PATCH 在校验后遇到反向生命周期转换，可恢复已被清空的字段 | P1 | **选中，优先修复** |
| 产品流程 | 陈旧编辑/删除缺少议题版本；活动时间未约束提前归档和过期报名 | P1 | 保留至后续轮次 |
| 交互/页面承诺 | 解锁弹窗叠加时单次 Esc 同时关闭底层表单 | P1 | 保留至下一轮 |
| 隐私/海报 | 混合地点可公开泄露会议凭证；中文凭证脱敏和极限布局不完整 | P1 | 保留至后续轮次 |
| 公网传输 | 共享口令在纯 HTTP 上缺少传输加密 | P0（部署边界） | 需域名/可信证书和 443 入站权限；不得宣称当前 80 为安全传输 |

选择理由：该缺陷会持久化违反状态机的数据，例如 `OPEN` 仍有分享人、`CLAIMED` 仍有排期或 `SCHEDULED` 仍有归档摘要。页面重新加载后无法仅靠界面纠正，属于核心数据完整性问题。

三方独立审查均确认本项；传输层候选风险更高，但当前缺少可信证书身份与外部 443 入站授权，且用户最后明确要求应用启用 80 端口，不能在本轮擅自改成只允许 HTTPS。其余可落地 P1 已登记，完成本轮后必须继续循环，不能作为停止条件。

### BUG-CONC-009 状态相关 PATCH 缺少写入时 CAS

复现：编辑实例读取一个 `CLAIMED` 议题并确认 `presenter` 可编辑后暂停；另一实例完成“退出认领”，把状态改为 `OPEN` 并清空分享人；编辑实例随后执行只带 `WHERE id = ?` 的 PATCH，成功把旧分享人写回，最终得到 `OPEN + presenter`。取消排期与排期字段、撤销归档与归档字段存在同类竞态。

修复规格：

- 标题、简介、发起人、标签是所有状态均可编辑的基础字段；只修改这些字段时继续按 ID 更新，允许与生命周期转换并发合并，不得回退 BUG-CONC-003 的列级更新能力。
- `presenter`、`scheduledAt`、`duration`、`room`、`meetingUrl`、`takeaway`、`materialUrl` 是状态相关字段；载荷只要包含任一状态相关字段，最终 UPDATE 必须同时匹配读取时的 `id + status`。
- 同一请求混合基础字段和状态相关字段时整体使用状态 CAS，不能只提交其中一部分。
- CAS 未命中后复查 ID：记录不存在返回 `404`；记录存在但状态已经变化返回带稳定错误码 `TOPIC_STATE_CONFLICT` 的 `409`，不得写入任何字段。
- 浏览器沿用生命周期冲突恢复：关闭陈旧编辑弹窗、显示明确提示、重新读取权威议题；不得把旧表单自动重放到新状态。

验收：

- 双实例确定性交错覆盖 `CLAIMED` 编辑分享人与退出认领：PATCH 返回 `409/TOPIC_STATE_CONFLICT`，最终 `OPEN + presenter=null`。
- 双实例确定性交错覆盖 `SCHEDULED` 编辑排期与取消排期：PATCH 返回 `409/TOPIC_STATE_CONFLICT`，最终 `CLAIMED` 且排期字段为空。
- 双实例确定性交错覆盖 `ARCHIVED` 编辑沉淀与撤销归档：PATCH 返回 `409/TOPIC_STATE_CONFLICT`，最终 `SCHEDULED` 且归档字段为空。
- 仅编辑标题与并发排期仍能成功合并，标题和完整排期均保留。
- 状态相关编辑期间记录被删除返回 `404`；全部既有 CRUD、生命周期、排序、报名、访问保护和跨端浏览器回归继续通过。

状态：Accepted。

实现与验证：

- PATCH 载荷包含任一状态相关字段时，最终写入使用读取状态作为 CAS 条件；混合载荷整体成功或整体冲突。
- CAS 未命中稳定区分 `404` 与 `409 TOPIC_STATE_CONFLICT`；浏览器关闭陈旧编辑、提示并同步权威状态。
- 双实例确定性交错覆盖退出认领、取消排期、撤销归档和并发删除；纯标题编辑与并发排期仍合并成功。
- 验证：32 项单元/API/安全/并发测试、桌面 15 项与移动 13 项 E2E 通过（另 2 项按设备设计跳过），生产构建和 0 漏洞审计通过。
- 实现提交：`1620fdd`。

## 15. 第 8 轮候选（2026-09-02）

| 审查方向 | 推举项 | 优先级 | 本轮决策 |
| --- | --- | --- | --- |
| 隐私/会议 | 混合 `room` 把会议链接或凭证原样暴露给匿名访问者，授权入口却识别不到 | P1 | **选中，优先修复** |
| 数据正确性 | 缺少议题 revision，陈旧编辑覆盖内容、陈旧删除抹掉新报名/排期 | P1 | 保留至下一轮 |
| 交互/可访问性 | 叠层 AccessModal 单次 Esc 同时关闭两层、丢草稿并遗留页面滚动锁 | P1 | 保留至后续轮次 |
| 活动语义 | 未开始活动可提前归档，已结束活动仍可报名/入会 | P1 | 保留至后续轮次 |
| 海报 | 中文/分段凭证脱敏遗漏与最大合法输入布局越界 | P1/P2 | 保留至后续轮次 |

选择理由：协调者只要把“说明 + URL/会议号/密码”误填进地点，匿名 API 与页面就会立即公开秘密，同时 `hasMeetingUrl=false` 使授权用户也无法使用加入入口。这是无需攻击条件即可触发的隐私泄露和核心功能双重失败。

状态：Accepted；详细规格与验证见 SPEC-004 FR-JOIN-003、Iteration 016。
